next-version: 1.0.0
branches:
  main:
    regex: main
    increment: Patch
    track-merge-target: false
merge-message-formats:
  '.*BREAKING CHANGE.*': major
  '.*FEATURE.*': minor
  '.*FIX.*': patch